# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/kava_wiki

# OpenAI API (only needed for translation script)
OPENAI_API_KEY=sk-your-openai-api-key

# Server Port (default: 5000)
PORT=5000

# Node Environment
NODE_ENV=development
